apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: default
spec:
  interval: 10m
  chart:
    spec:
      chart: "./helm/example-app-1/"
      version: '0.1.0'
      sourceRef:
        kind: GitRepository
        name: example-app-1
        namespace: flux-system
      interval: 10m
  values:
    replicaCount: 2
